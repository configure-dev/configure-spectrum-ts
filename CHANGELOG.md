# Changelog

## 0.1.0-preview.0

- Initial private preview scaffold for `@configure-ai/spectrum-ts`.
- Adds the `withConfigure()` adapter, explicit store contract, dev-only `memoryStore()`, framework-neutral completion helper, and typed Spectrum handler context.
